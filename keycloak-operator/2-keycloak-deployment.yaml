# Deploys the Keycloak server using the Keycloak operator objects

apiVersion: k8s.keycloak.org/v2alpha1
kind: Keycloak
metadata:
  name: keycloak
  namespace: keycloak-operator
  labels:
    app: sso
spec:
  db:
    vendor: postgres
    usernameSecret:
      name: postgres.keycloak-postgresql.credentials.postgresql.acid.zalan.do
      key: username
    passwordSecret:
      name: postgres.keycloak-postgresql.credentials.postgresql.acid.zalan.do
      key: password
    host: keycloak-postgresql.keycloak.svc.cluster.local
    database: keycloak
    port: 5432
  instances: 1
  hostname: 
    hostname: keycloak.adahinh.net
    strict: false
    strictBackchannel: false
  http:
    httpEnabled: true
