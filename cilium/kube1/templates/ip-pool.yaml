apiVersion: "cilium.io/v2"
kind: CiliumLoadBalancerIPPool
metadata:
  name: "default-pool"
  labels:
    cluster: kube1
spec:
  disabled: false
  blocks:
    - cidr: "10.246.0.0/16"
  serviceSelector:
    matchExpressions:
      - {key: useverything, operator: NotIn, values: [useitall]}